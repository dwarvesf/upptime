apiVersion: v1
kind: Service
metadata:
  name: nginx-api-proxy-service
  namespace: upptime
spec:
  selector:
    app: nginx-api-proxy
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP