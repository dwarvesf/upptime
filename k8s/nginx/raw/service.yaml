apiVersion: v1
kind: Service
metadata:
  name: nginx-raw-proxy-service
  namespace: upptime
spec:
  selector:
    app: nginx-raw-proxy
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP